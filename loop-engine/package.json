{
  "name": "@powerclub/loop-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous dev loop engine — Hermes + Auri + Playwright + Fake-User. Reactive sub-agent orchestrator with Ralph Wiggum circuit breaker.",
  "main": "dist/orchestrator.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "ts-node --transpile-only src/demo.ts",
    "demo:built": "npm run build && node dist/demo.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
